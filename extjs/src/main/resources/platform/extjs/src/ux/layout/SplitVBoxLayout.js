Ext.define('Ext.ux.layout.SplitVBoxLayout', {
    extend: 'Ext.layout.container.VBox',
    initComponent : function(){ 
        var me = this; 
        Ext.apply(me, { 
        	
        }); 
        me.callParent(arguments); 
    } 
});